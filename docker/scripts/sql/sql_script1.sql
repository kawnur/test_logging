create table test1 (
    timepoint timestamp without time zone,
    service_ip varchar(30),
    field1 varchar(30),
    field2 varchar(30),
    field3 varchar(30),
    log_message varchar(500)
);

create table test2 (
    timepoint timestamp without time zone,
    field1 varchar(30),
    field2 varchar(30),
    field3 varchar(30),
    log_message varchar(500)
);

create table test3 (
    timepoint timestamp without time zone,
    field1 varchar(30),
    field2 varchar(30),
    field3 varchar(30),
    log_message varchar(500)
);

create table test4 (
    timepoint timestamp without time zone,
    service_ip varchar(30),
    field1 varchar(30),
    field2 varchar(30),
    field3 varchar(30),
    log_message varchar(500)
);
